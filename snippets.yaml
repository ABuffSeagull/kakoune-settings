scaffold: |
  <template>
    $0
  </template>

  <script>
  export default {

  }
  </script>

  <style>

  </style>

newform: |
  <template>
    <v-form ref="form" @submit.prevent="submit">
      $0
      <v-btn @click="$router.go(-1)">Back</v-btn>
      <v-btn type="submit">Continue</v-btn>
    </v-form>
  </template>

  <script>
  export default {
    methods: {
      async submit() {
        if (!this.$refs.form.validate()) return;
      }
    }
  }
  </script>

theme: ${props => props.theme.$0}

newtest: |
  import faker from 'faker';

  describe('$0 form', () => {
    beforeEach(() => {
      cy.visit('$1');
    });

    it('should $2', () => {
     $3
    })
  });

it: |
  it('should $0', () => {
    $1
  });

datatype: cy.get('[data-cy=$0]').type(faker.$1);

test: |
  test('should $0', () => {
    $1
  });

wrap: const wrapper = shallow(<$1 />);

data: |
  data: () => ({
    $0
  }),
